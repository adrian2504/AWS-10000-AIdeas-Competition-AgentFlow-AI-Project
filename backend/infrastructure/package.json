{
  "name": "agentflow-infrastructure",
  "version": "1.0.0",
  "description": "AWS CDK infrastructure for AgentFlow",
  "main": "bin/app.js",
  "scripts": {
    "build": "echo 'No build needed'",
    "deploy": "cdk deploy --all --require-approval never",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "destroy": "cdk destroy --all"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.100.0",
    "constructs": "^10.0.0"
  },
  "devDependencies": {
    "aws-cdk": "^2.100.0"
  }
}
